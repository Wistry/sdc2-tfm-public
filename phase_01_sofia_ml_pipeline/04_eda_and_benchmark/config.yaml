data:
  candidates_path: "../03_candidate_dataset/outputs/baseline_current_full/candidates_sofia_only.csv"
  label_column: "clean_label"
  positive_label: 1
  negative_label: 0
  ambiguous_label: -1

experiment:
  random_state: 42
  test_size: 0.2
  n_splits: 5
  n_repeats: 5
  scoring_main: "average_precision"

outputs:
  eda_dir: "outputs/eda"
  benchmark_dir: "outputs/benchmark"
  summary_dir: "outputs/summary"
  reports_dir: "reports"
